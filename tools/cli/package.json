{
  "name": "@ccms/cli",
  "description": "command line interface created to simplify our work with the project",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "cli": "./bin.js"
  },
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .js,.ts,.tsx",
    "clean": "rm -rf node_modules & rm -rf dist"
  },
  "dependencies": {
    "@ccms/node": "workspace:*",
    "commander": "9.0.0",
    "concurrently": "7.0.0",
    "dotenv": "14.3.2"
  },
  "devDependencies": {
    "tsup": "5.11.11"
  }
}
