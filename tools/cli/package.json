{
  "name": "@ccms/cli",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "cli": "./bin.js"
  },
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "clean": "rm -rf .turbo & rm -rf node_modules & rm -rf dist"
  },
  "dependencies": {
    "@ccms/config": "*",
    "commander": "8.3.0",
    "dotenv": "10.0.0"
  },
  "devDependencies": {
    "tsup": "5.11.10"
  }
}
