{
  "name": "@ccms/cli",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "cli": "src/index.js"
  },
  "scripts": {
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "dependencies": {
    "@ccms/config": "*",
    "commander": "8.3.0",
    "dotenv": "10.0.0"
  }
}
